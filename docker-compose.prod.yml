services:
  postgres:
    ports: []

  api:
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      Cors__Origins__0: http://localhost
      Cors__Origins__1: http://127.0.0.1
    ports:
      - "5122:8080"

  frontend:
    build:
      target: production
    environment: {}
    ports:
      - "80:80"
    volumes: []
