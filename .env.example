# Senha do banco Postgres usado pelo docker-compose.
POSTGRES_PASSWORD=postgres

# Token da API https://brapi.dev (Dashboard -> API Keys).
# Sem token, recursos que dependem de cotacao podem falhar.
BRAPI_TOKEN=